# Handoff Document

**Created:** 2026-05-21 23:00
**Topic:** Story 2: Local SLM Hello-World (Qwen2.5-7B-Instruct)
**Status:** Ready to Start

---

## Context Summary

Vietnamese GraphRAG thesis project. Story 1 (Vietnamese KG Population) is complete. Starting Story 2: integrate a local Small Language Model for offline inference.

---

## Completed Work (Story 1 — Done)

- [x] Ingested 1000 Vietnamese Wikipedia pages into Neo4j
- [x] Wikidata enrichment: 977 QIDs, 3,062 semantic relations
- [x] Schema migration: primary labels only, native vector index (384d cosine)
- [x] Cleaned noise entities: 3,324 typed remain (Person:2574, Org:339, Location:327, Work:84)
- [x] Event label support added
- [x] Multi-hop traversal validated
- [x] Default config updated to Vietnamese
- [x] Ingest pipeline aligned with new schema

---

## Pending Work (Story 2)

- [ ] Add `transformers`, `bitsandbytes`, `accelerate` to pyproject.toml
- [ ] Create `src/local_llm.py` — load Qwen2.5-7B-Instruct in 4-bit NF4
- [ ] Add `MODEL_MODE=local|api` toggle in `src/config.py`
- [ ] Test: Vietnamese question to coherent output from local model

---

## Key Context

### Hardware
- GPU: NVIDIA RTX 4060 Laptop, 8GB VRAM (7.8GB free)
- Strategy: Qwen2.5-7B-Instruct in 4-bit NF4 quantization (~5GB VRAM)

### Next Steps

1. `uv add transformers bitsandbytes accelerate`
2. Create `src/local_llm.py`:
   - BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=float16)
   - Model: Qwen/Qwen2.5-7B-Instruct
   - Chat template support for tool-calling
3. Add `model_mode: str = "api"` to Settings with validator
4. Wire into `src/llm.py` — when model_mode == "local", use local_llm
5. Quick test to verify generation

### Important Notes

- Neo4j runs as system service
- EMBEDDING_BACKEND=local (sentence-transformers)
- NER_BACKEND=underthesea
- Dataset at data/viet-wikipedia/ (NOT in git)
- Run scripts with -m flag: uv run python -m scripts.xxx
- Roadmap: plans/roadmap.md
