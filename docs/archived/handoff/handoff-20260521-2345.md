# Handoff Document

**Created:** 2026-05-21 23:45
**Topic:** Story 5: QLoRA Fine-tuning
**Status:** Ready to Start

---

## Context Summary

Vietnamese GraphRAG thesis project. Stories 1-4 complete. The system has:
- 1000 Vietnamese Wikipedia pages in Neo4j with typed entities
- Local Qwen2.5-7B-Instruct running in 4-bit NF4
- ReAct agent loop with 4 graph tools (kg_schema, kg_query, text_search, get_passage)
- Dataset generation pipeline producing ViWiki-MHR JSONL

---

## Completed Work (Stories 1-4 — Done)

- [x] Vietnamese KG populated (1000 pages, 19K chunks, 3324 typed entities)
- [x] Local SLM: Qwen2.5-7B-Instruct in 4-bit NF4 (~5GB VRAM)
- [x] ReAct agent loop: 6-iter cap, JSON-based thought/action/observation
- [x] Dataset pipeline: 2-hop/3-hop walks, templates, LLM rewrite, QC filter
- [x] All tests pass at 83%+ coverage, clean lint/types

---

## Pending Work (Story 5)

- [ ] Create `src/training/prepare_data.py` — convert ViWiki-MHR dataset to:
  - Text2Cypher format: (question, cypher) pairs for Cypher generation adapter
  - Agent format: (question, thought/action/observation trace) for ReAct conformance
- [ ] Create `src/training/qlora_train.py` — QLoRA training with PEFT/TRL:
  - 4-bit NF4 base, LoRA rank 32, alpha 64
  - Training on Text2Cypher + agent action format
  - Gradient checkpointing for 8GB VRAM constraint
- [ ] Create `src/training/dpo_align.py` — DPO alignment pass:
  - Preferred: correct Cypher + grounded answers
  - Rejected: invalid Cypher + hallucinated answers
- [ ] Create `src/training/evaluate.py` — evaluation harness:
  - Exact Match (EM) on Cypher generation
  - F1 on answer extraction
  - Joint score on ViWiki-MHR test split

---

## Key Context

### Hardware
- GPU: NVIDIA RTX 4060 Laptop, 8GB VRAM
- Strategy: QLoRA (4-bit NF4 base + LoRA adapters)
- Must use gradient checkpointing to fit in VRAM

### Training Config
- Base: Qwen/Qwen2.5-7B-Instruct
- LoRA: rank=32, alpha=64, target_modules=["q_proj", "k_proj", "v_proj", "o_proj"]
- Batch size: 1 (with gradient accumulation 8-16)
- Learning rate: 2e-4 with cosine schedule
- Epochs: 3

### Dataset
- Generate first: `uv run python -c "from src.dataset_gen import generate_dataset; generate_dataset()"`
- Output: `data/viwiki_mhr.jsonl`
- Split: 90% train, 10% test

### Dependencies to Add
- `peft>=0.12.0`
- `trl>=0.11.0`
- `datasets` (already present)

### Important Notes
- Neo4j runs as system service
- EMBEDDING_BACKEND=local (sentence-transformers)
- NER_BACKEND=underthesea
- Branch: fix/critical-bugs-and-coverage
- Run scripts with: `uv run python -m src.training.prepare_data`

---

## Next Steps (Priority Order)

1. Generate the dataset first (run pipeline against live graph)
2. Implement data preparation (convert to training formats)
3. Implement QLoRA training script
4. Train Text2Cypher adapter
5. Run evaluation harness
6. DPO alignment pass (if time permits)
