# Handoff Document

**Created:** 2026-05-24 12:00
**Topic:** Update HF dataset source to Keithsel/viwiki-20260523 + download data
**Status:** In Progress

---

## Context Summary

Switched the project's HuggingFace dataset source from the old `wikimedia/wikipedia` (config `20231101.vi`) to the new custom dataset `Keithsel/viwiki-20260523`. Downloaded the full cleaned dataset (1.6M articles) to local disk. Data has not yet been ingested into Neo4j.

---

## Completed Work

- [x] Verified new dataset schema matches existing code expectations (`id`, `url`, `title`, `text`)
- [x] Updated `src/ingest.py`: added `dataset_id` parameter, changed default config to `cleaned`
- [x] Updated `src/main.py`: added `dataset_id` field to `HFDatasetIngestRequest`, passed through to both call sites
- [x] Created `scripts/download_dataset.py` CLI script
- [x] Downloaded full dataset (1,607,382 articles) to `data/viwiki-cleaned/`
- [x] Verified all existing tests still pass

---

## Pending Work

- [ ] Ingest downloaded data into Neo4j (start with a batch, e.g. 1000-5000 articles)
- [ ] Verify ingestion quality (entities, chunks, embeddings)
- [ ] Update `scripts/ingest_local_dataset.py` to use new default path
- [ ] Consider adding `dataset_id` to `_JobState` for job tracking
- [ ] Update tests that reference old config name `20231101.vi` (cosmetic, tests still pass)
- [ ] Add `data/viwiki-cleaned/` to `.gitignore` if not already excluded

---

## Key Decisions Made

| Decision | Rationale | Alternatives Considered |
|----------|-----------|------------------------|
| Use `cleaned` config as default | Pre-stripped wiki markup, ready for NER/chunking | `raw` config (has markup, needs extra parsing) |
| Add `dataset_id` as parameter with default | Backward-compatible, allows overriding for other datasets | Hardcoding the new dataset ID |
| Keep old test config names | Tests mock `load_dataset` anyway, values are arbitrary | Updating all test fixtures |

---

## Current State

### Files Modified
- `src/ingest.py` — new `dataset_id` param, updated defaults
- `src/main.py` — new `dataset_id` field in request model, passed to ingest calls

### Files Created
- `scripts/download_dataset.py` — CLI to download HF dataset to disk
- `data/viwiki-cleaned/` — 1.6M articles (Arrow format, ~4 shards)

### Dependencies/Blockers
- Neo4j must be running for ingestion (`sudo systemctl start neo4j`)
- Full ingestion of 1.6M articles will take significant time; batch approach recommended

---

## Next Steps

1. Start Neo4j and ingest a test batch:
   ```bash
   sudo systemctl start neo4j
   uv run python scripts/ingest_local_dataset.py --path data/viwiki-cleaned --limit 1000
   ```
2. Verify graph data quality in Neo4j Browser
3. Scale up ingestion (larger batches or background job via API)
4. Run evaluation pipeline against new data

---

## Important Notes

- The new dataset has two configs: `cleaned` (plain text) and `raw` (wiki markup)
- Dataset fields are identical to old source: `id`, `url`, `title`, `text`
- All 22 relevant tests pass; coverage warning is pre-existing (32% < 75% threshold)
- Branch: `feat/viwiqa-retrieval-improvements`

---

## Resources

- Dataset: https://huggingface.co/datasets/Keithsel/viwiki-20260523
- Ingest API docs: `POST /ingest/hf` and `POST /ingest/hf/jobs`
- Local ingest script: `scripts/ingest_local_dataset.py`
