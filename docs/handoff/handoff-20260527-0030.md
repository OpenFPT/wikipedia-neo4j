# Handoff Document

**Created:** 2026-05-27 00:30
**Topic:** Research Paper Implementations — Commit, Fix, Wire, Evaluate
**Status:** In Progress

---

## Context Summary

Continuing from handoff-20260526-2230. Applied 7 research papers to the GraphRAG system. This session focused on committing all changes (split into logical commits) and fixing the ViDeBERTa NER type classification bug.

---

## Completed Work

- [x] Split uncommitted changes into 6 logical commits on `fix/ner-data-quality`:
  1. `9644dfe` feat(ner): add ViDeBERTa backend and text grounding utilities
  2. `8865930` feat(retrieve): hybrid retrieval with weighted RRF fusion
  3. `5b888a9` feat(community): Leiden community detection with igraph
  4. `f451a72` feat(agent): multi-strategy retrieval and inference-time scaling
  5. `a188f37` feat(eval): evaluation pipeline improvements and reranker fix
  6. `f4d0106` docs: research report, embedding benchmark, and link prediction scripts
- [x] Diagnosed NER type classification bug: NlpHUST model outputs `B-LOCATION`, `B-PERSON`, `B-ORGANIZATION`, `B-MISCELLANEOUS` but `_NER_TYPE_MAP` only had short forms (`PER`, `ORG`, `LOC`, `MISC`)
- [x] Wrote fix: extend `_NER_TYPE_MAP` with full-name variants (PERSON, ORGANIZATION, LOCATION, MISCELLANEOUS)
- [x] Verified fix passes all 33 NER tests

---

## Pending Work

- [ ] **Commit the NER fix** — `src/ner.py` has uncommitted change (extend `_NER_TYPE_MAP` with full-name labels). Commit message: `fix(ner): map full-name BIO labels from NlpHUST model`
- [ ] Wire `hybrid_retrieve()` into API layer (`src/main.py` endpoints)
- [ ] Wire community retrieval into wRRF pipeline (channel weighted at 0.15)
- [ ] Run community detection on live Neo4j graph (`scripts/build_communities.py --limit 1000`)
- [ ] Run full embedding benchmark with larger corpus (50+ queries)
- [ ] Test inference-time scaling on hard multi-hop questions (AGENT_N_TRAJECTORIES=3)
- [ ] Run link prediction on candidate pairs (`scripts/predict_links.py --limit 100 --dry-run`)
- [ ] Evaluate end-to-end improvement on ViQuAD 2.0 / viwiki_mhr

---

## Key Decisions Made

| Decision | Rationale | Alternatives Considered |
|----------|-----------|------------------------|
| 6 logical commits | Easier to review, revert, cherry-pick | Single large commit |
| Fix NER via extending type map | Root cause: model uses full label names, map only had abbreviations | Normalize labels before lookup |
| Execution order: commit → fix → wire → evaluate | Dependencies flow naturally | Parallel execution (risky) |

---

## Current State

### Files Modified (uncommitted)
- `src/ner.py` — `_NER_TYPE_MAP` extended with `PERSON`, `ORGANIZATION`, `LOCATION`, `MISCELLANEOUS` keys

### Branch
- `fix/ner-data-quality` — 6 new commits ahead of previous state

### Dependencies/Blockers
- Neo4j must be running for community detection and link prediction
- GPU recommended for embedding benchmark
- Gemini API keys needed for link prediction

---

## Next Steps

1. **Commit NER fix** — `git add src/ner.py && git commit -m "fix(ner): map full-name BIO labels from NlpHUST model"`
2. **Wire hybrid_retrieve into API** — add/replace endpoint in `src/main.py`
3. **Wire community retrieval** — connect community channel in `hybrid_retrieve()`
4. **Run community detection** — needs Neo4j with ingested data
5. **End-to-end evaluation** — run `src/evaluation.py` to measure improvement

---

## Important Notes

- The `wrrf_weight_community` (0.15) is configured but community retrieval isn't wired into `hybrid_retrieve()` yet
- `run_agent_scaled` is the new entry point for inference scaling; `agent_query` remains unchanged
- All tests pass at 75.42% coverage
- The planned task-to-skill mapping for remaining work:
  - Wire hybrid_retrieve → `superpowers:executing-plans`
  - Wire community retrieval → `superpowers:executing-plans`
  - End-to-end eval → `superpowers:verification-before-completion`

---

## Resources

- Research report: `docs/research/applicable-papers-2024-2025.md`
- Previous handoff: `docs/handoff/handoff-20260526-2230.md`
- NlpHUST model labels: `{B,I}-{LOCATION,MISCELLANEOUS,ORGANIZATION,PERSON}` + `O`
